{
  "$schema": "http://json-schema.org/draft-04/schema",
  "definitions": {
    "jobBoardsForApp": {
      "type": "object",
      "properties": {
        "app": {
          "type": "string",
          "description": "the app that is hosting these boards"
        },
        "jobBoards": {
          "$ref": "jobBoards.json"
        }
      },
      "additionalProperties": false,
      "required": [
        "app",
        "jobBoards"
      ]
    }
  },
  "type": "array",
  "items": {
    "$ref": "#definitions/jobBoardsForApp"
  },
  "description": "An array of job boards rolled up across all producing apps"
}
