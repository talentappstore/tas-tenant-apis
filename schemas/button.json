{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "object",
	"description": "the logical information needed to populate a button, excluding style information (which is down to the app rendering the button)",
	"properties": {
		"verb": {
			"type": "string",
			"description": "a string indicating what the button will do, used as the label"
		},
		"behavior": {
			"description": "when the button is clicked, either the user is redirected to the uri, or a popup renders the uri inside a modal (inside an iframe)",
			"enum": ["redirect", "modal"]
		},
		"uri": {
			"type": "string",
			"format": "uri",
			"description": "the url that the button will redirect the user to when the button is pressed, or use to populate an iframe inside a modal"
		},
		"appearance": {
		  "description": "the appearance of the button, as per http://getbootstrap.com/css/#buttons",
		  "enum": [ "default", "primary", "success", "info", "warning", "danger" ]
		},
		"disabled": {
			"type": "boolean",
			"description": "true if the button should appear disabled"
		}
	},
	"additionalProperties": false
}