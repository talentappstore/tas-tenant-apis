{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "definitions": {
    "key": {
      "type": "string",
      "description": "the engagement app's unique (within that app) and immutable key for this engagement type"
    },
    "title": {
      "type": "string",
      "description": "label for this engagement type"
    },
    "daysToExpire": {
      "type": "integer",
      "description": "the number of elapsed days before this engagements result is no longer valid - pass 0 to never expire"
    },
    "isPassFail": {
      "type": "boolean",
      "description": "whether this engagement results in an unambiguous pass/fail result, as stored in assessmentDetail.overallResult.passedIndicator"
    },
    "candidateSeesResults": {
      "type": "boolean",
      "description": "whether the candidate should have visibility of the results of this engagement, including pass/fail"
    },
    "canReuse": {
      "type": "boolean",
      "description": "whether an unexpired engagement result on one job application can be reused when the candidate makes another job application"
    },
    "candidateDescription": {
      "type": "string",
      "description": "A description, worded for the candidate, of the purpose of this engagement. Markdown format. e.g.: Video interview is a short procedure you can complete on your own phone. We like to see who we're hiring."
    },
    "appCommunicatesDirectlyToCandidate": {
      "type": "boolean",
      "description": "True if the app communicates directly with the candidate when an engagement is started. In this case the hub will not send the candidate an email message."
    },
    "engagementType": {
      "type": "object",
      "properties": {
        "key": {
          "$ref": "#/definitions/key"
        },
        "title": {
          "$ref": "#/definitions/title"
        },
        "daysToExpire": {
          "$ref": "#/definitions/daysToExpire"
        },
        "isPassFail": {
          "$ref": "#/definitions/isPassFail"
        },
        "candidateSeesResults": {
          "$ref": "#/definitions/candidateSeesResults"
        },
        "canReuse": {
          "$ref": "#/definitions/canReuse"
        },
        "candidateDescription": {
          "$ref": "#/definitions/candidateDescription"
        },
        "appCommunicatesDirectlyToCandidate": {
          "$ref": "#/definitions/appCommunicatesDirectlyToCandidate"
        },
	    "image": {
	      "$ref": "engagementRead.json#/definitions/imageWriteUrl"
	    }
      },
      "required": [
        "key",
        "title",
        "daysToExpire",
        "isPassFail",
        "candidateSeesResults",
        "canReuse",
        "candidateDescription",
        "appCommunicatesDirectlyToCandidate",
        "image"
      ]
    }
  },
  "type": "array",
  "items": {
    "$ref": "#/definitions/engagementType"
  }
}