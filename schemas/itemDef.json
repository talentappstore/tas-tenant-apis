{
    "$schema": "http://json-schema.org/draft-04/schema",
    "type": "object",
    "description": "A package of information including a reference to, or embedded json schema that describes the items pertaining to a candidate or application - see separate doc.",
    "properties": {
        "name": {
            "type": "string",
            "description": "the identifier for this item"
        },
        "embeddedSchemaFragment": {
            "$ref": "jsonSchemaSchema.json"
        }
    },
    "required": [
        "name"
    ],
    "additionalProperties": false
}