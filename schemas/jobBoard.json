{
  "$schema": "http://json-schema.org/draft-04/schema",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "immutable id for this board"
    },
    "label": {
      "type": "string",
      "description": "label for this board, e.g. Graduate recruitment site"
    },
    "baseUri": {
      "type": "string",
      "format": "uri",
      "description": "base uri for this board, e.g. used to prefix job links"
    },
    "jobUriTemplate": {
      "type": "string",
      "description": "the URI template (relative) that the board uses as job links - some subset of id, code and safe title may need to be replaced, and a tracker argument may be added"
    }
  },
  "required": [
    "id",
    "label",
    "baseUri",
    "jobUriTemplate"
  ],
  "additionalProperties": false
}